# Hi Ola here. I created this photo search app using React.

## Available Scripts

In the project directory, you can run:

### `npm start`

### `npm run build`

But make sure to run this command first to download all required module:

### `npm install`
